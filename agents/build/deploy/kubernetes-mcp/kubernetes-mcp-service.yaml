apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp
  namespace: ai-platform
spec:
  type: ClusterIP

  selector:
    app: kubernetes-mcp

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
